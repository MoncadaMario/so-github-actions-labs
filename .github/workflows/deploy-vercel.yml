name: Deploy to Vercel

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      - name: Install dependencies
        working-directory: ./so-cicd-app
        run: npm ci

      - name: Build app
        working-directory: ./so-cicd-app
        run: npm run build

      - name: Simulated deploy to Vercel
        run: echo "Deploy a Vercel simulado desde GitHub Actions (el deploy real lo hace la integración de Vercel con GitHub)."
